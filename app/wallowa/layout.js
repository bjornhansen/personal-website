export const metadata = {
  title: 'Wallowa Camp | Bjorn Hansen',
  description:
    'An explorable campsite in the Wallowa Mountains — a 3D prototype. Bear will show you around.',
  robots: { index: false, follow: true },
}

export default function WallowaLayout({ children }) {
  return children
}
